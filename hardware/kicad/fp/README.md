# Footprint Library Notes

Custom footprints should only be introduced when:

- the package is not available in standard KiCad libraries
- enclosure-specific mounting geometry requires local control
- USB-C cable anchoring or thermal-pad chassis interfaces need custom land patterns

Rev-A expected custom footprint candidates:

- captive USB-C cable/mechanical anchor
- enclosure interface pads or thermal slug interface hardware

